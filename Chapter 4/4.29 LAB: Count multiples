#include <iostream>
using namespace std;

int main() 
{

   int low, high, x;
   int count = 0;
   
   cin >> low;
   cin >> high;
   cin >> x;
   
   for (int i = low; i <= high; ++i) 
   {
      if  (i % x == 0) 
      {
         count++;
      }
   }

   cout << count << endl;

   return 0;
}
